//单选开关型按钮
(function (jQuery) {
    jQuery.fn.extend({
        lvsbloguser: function (token, idxid, curdata) {
            var userbox = $(this);
            return this;
        }
    });
})(jQuery);
